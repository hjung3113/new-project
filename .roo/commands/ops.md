---
description: Route operations and observability work
argument-hint: <logging, metrics, retry, worker lifecycle, shutdown, or dashboard task>
mode: orchestrator
---

Use the `workflow-ops-observability` skill for $ARGUMENTS.

Apply `.roo/rules-orchestrator/rules.md`, `.roo/rules/phase-gate.md`, and `.roo/rules-ops-observability/rules.md` first. Stay on `ops-observability` only for structured logs, metrics, processing events, retry boundaries, worker polling, graceful shutdown, dashboards, or runbooks.
