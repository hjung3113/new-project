# Diagnose Rules

- Reproduce before fixing.
- Minimize the failing case.
- State hypotheses and evidence.
- Trace ETL data bugs through each stage, not only at the final output.
- Add a regression test that fails before the fix.
- Report Cause, Evidence, Fix, Files affected, and Tests run.

